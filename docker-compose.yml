services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CONVEX_URL
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
